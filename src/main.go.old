package main

import (
	"emulator/src/cpu"
	"emulator/src/keyboard"
	"emulator/src/screen"
)

func main() {
	var s = screen.SCREEN{}
	var c = cpu.CPU{Display: &s}

	s.Initialize()

	running := true
	for running {
		running = keyboard.Event()
		c.Decrease()
	}
	s.Destroy()
}
